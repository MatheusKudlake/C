#include <stdio.h>

int main()
{
    int lin, col;
    printf("Insira a quantidade de linhas da matriz:\n");
    scanf("%d", &lin);
    printf("Insira a quantidade de colunas da matriz:\n");
    scanf("%d", &col);

    int matriz[lin][col];

    printf("Insira os ")

    for(int l=0; l<lin; l++){
        for(int c=0; c<col; c++){
            scanf("")
        }
    }
    return 0;
}
